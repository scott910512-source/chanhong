__pycache__/
*.py[cod]
.venv/
venv/
.DS_Store

# 실행하면서 생기는 시세 캐시 (quotes.seed.json 은 데모용이라 커밋)
data/cache/quotes.json
data/cache/actions-quotes.json

# 생성물
web/dashboard.html
*.local.*
portfolio.json

# 앱에서 저장한 설정 오버라이드 (개인 설정이라 커밋 안 함)
data/settings.overrides.json

# 아이콘 재생성할 때만 쓰는 playwright
node_modules/
package-lock.json
